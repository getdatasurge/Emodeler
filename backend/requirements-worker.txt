# Additional deps for the production EnergyPlus worker image (spec Ch 9.5).
-r requirements.txt
celery==5.4.0
redis==5.2.1
# IDF generation against bundled DOE prototypes:
eppy==0.5.63
# geomeppy intentionally omitted: it pulls shapely+matplotlib (the build's pip
# failure) and is only needed for >30% geometry vertex-scaling, which the inline
# run path doesn't use. Re-add (with pinned shapely/numpy) when wiring scaling.
# Server-side PDF rendering for the canonical branded report:
# weasyprint==63.1
# Object storage (Cloudflare R2 / S3):
boto3==1.35.90
